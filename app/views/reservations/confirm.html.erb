<div class="container py-4">
  <h1 class="reservation mb-4">予約内容の確認</h1>

  <div class="form-content p-6">
    <div class="row mb-2">
      <div class="col-3"><strong>チェックイン</strong></div>
      <div class="col-3 text-right"><%= @reservation.check_in.strftime('%Y/%m/%d') %></div>
    </div>
    <div class="row mb-2">
      <div class="col-3"><strong>チェックアウト</strong></div>
      <div class="col-3 text-right"><%= @reservation.check_out.strftime('%Y/%m/%d') %></div>
    </div>
    <div class="row mb-2">
      <div class="col-3"><strong>日数</strong></div>
      <div class="col-3 text-right"><%= @reservation.total_days %>日</div>
    </div>
    <div class="row mb-2">
      <div class="col-3"><strong>人数</strong></div>
      <div class="col-3 text-right"><%= @reservation.number_of_guests %>人</div>
    </div>
    <div class="row mb-2">
      <div class="col-3"><strong>お支払い金額</strong></div>
      <div class="col-3 text-right"><%= number_to_currency(@reservation.total_price, unit: "¥", precision: 0, format: "%u%n") %></div>
    </div>
    <p class="mt-4">上記内容で正しければ「確定」ボタンを押してください。</p>
    
<%= form_with(model: [@room, @reservation], local: true) do |form| %>
  <%= form.hidden_field :check_in, value: @reservation.check_in %>
  <%= form.hidden_field :check_out, value: @reservation.check_out %>
  <%= form.hidden_field :number_of_guests, value: @reservation.number_of_guests %>
  
  <div class="mb-2">
    <%= form.submit '予約を確定', class: 'btn btn-primary btn-block' %>
  </div>
  <div>
    <%= link_to 'キャンセル', room_path(@room), class: 'btn btn-secondary btn-block' %>
  </div>
<% end %>
  </div>
</div>
